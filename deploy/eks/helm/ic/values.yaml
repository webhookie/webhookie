# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

app: webhookie # metered container product
region:
vpcId:

ic: #ingress controller
  name: 'ic'
  replicas: 1
  imageName: 'docker.io/amazon/aws-alb-ingress-controller'
  imageVersion: 'v2.3.0'
